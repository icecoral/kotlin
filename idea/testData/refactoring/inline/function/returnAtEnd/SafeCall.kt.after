fun f(s: String?) {
    s?.let {
        println(1)
        it.hashCode() * 1
    }
    s?.substring(1)?.let {
        println(2)
        it.hashCode() * 2
    }
    val s1 = s?.let {
        println(3)
        it.hashCode() * 3
    }
    val s2 = s?.substring(1)?.let {
        println(4)
        it.hashCode() * 4
    }

    val s3 = s?.substring(1)
    s3?.let {
        println(2)
        it.hashCode() * 2
    }
    s3?.let {
        println(3)
        it.hashCode() * 3
    }
}